# This file exists so Git tracks the assets/images/ folder.
# Actual image files are excluded via .gitignore.
# When deploying, upload your images to this folder manually
# or configure a cloud storage service (e.g. AWS S3).
